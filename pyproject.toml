[project]
name = "health-context"
version = "0.1.0"
description = "Build LLM-friendly Markdown context from personal data."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0",
    "requests>=2.31",
]

[project.scripts]
health-context = "health_context:main"
